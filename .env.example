# Django settings
DEBUG=True

# SECRET_KEY: Required in production (DEBUG=False)
# For development, it will auto-generate if not set
# Generate a secure key with: python -c "import secrets; print(secrets.token_urlsafe(50))"
SECRET_KEY=your-secret-key-here-change-in-production

# Database settings (development)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=doacoes_db
DB_USER=postgres
DB_PASSWORD=postgres123
DB_HOST=localhost
DB_PORT=5432

# Database settings (production - Render)
# DATABASE_URL=postgresql://user:password@host:port/database

# Allowed hosts (comma-separated)
ALLOWED_HOSTS=localhost,127.0.0.1

# CORS settings (comma-separated)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# AWS S3 settings (production only, when DEBUG=False)
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_STORAGE_BUCKET_NAME=your-bucket-name
# AWS_S3_REGION_NAME=sa-east-1

# Admin user creation (optional, for production deployment)
# ADMIN_USER=admin
# ADMIN_EMAIL=admin@example.com
# ADMIN_PASSWORD=secure-password
